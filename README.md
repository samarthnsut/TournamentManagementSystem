# Tournament Management Frontend (Static MVP)

Scaffolded Next.js + TypeScript + Tailwind starter for the tournament management frontend. This is a static MVP skeleton with sample pages and hardcoded data.

To run locally:

```bash
npm install
npm run dev
```
